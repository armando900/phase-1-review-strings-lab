// Write your code in this file!
const currentUser = 'name';

const welcomeMessage = 'Welcome to Flatbook, ${name} !';

const excitedWelcomeMessage = welcomeMessage.toUpperCase();

const shortGreeting = `Welcome, ${name.slice(0)}!`;

